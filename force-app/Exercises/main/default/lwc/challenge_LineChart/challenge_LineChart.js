import { LightningElement } from 'lwc';

export default class Challenge_LineChart extends LightningElement {
   /* @api apiURL ="";
    error;
    @track data;
    fetch(ApiURL)
        .then((result => {
            if(!response.ok) {
                this.error = response;
            }
            return response.json();
        }
        .then((jsonResponse) => {
            this.someData = jsonResponse;
        }
        .catch(error => {
            this.error = error;
            this.data = undefined;
        });
        */

}